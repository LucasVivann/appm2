name: app2
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0' # ou '>=3.0.0 <4.0.0' dependendo da versão do Dart/Flutter que você está utilizando

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0+1
  geolocator: ^8.2.1
  google_maps_flutter: ^2.2.2
  http: ^0.13.4
  provider: ^6.0.4
  image_picker: ^0.8.4+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0



flutter:
  uses-material-design: true
